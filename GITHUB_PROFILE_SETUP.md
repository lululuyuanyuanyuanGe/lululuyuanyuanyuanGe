# GitHub profile setup

This file records the profile settings that cannot be represented by the profile README.
The pinned repository set remains unchanged.

## Public profile fields

- **Name:** `Luyuan Ge | 戈麓源`
- **Bio:** `Artificial Intelligence Systems / Infra | Inference serving, key-value cache runtimes, distributed training, real-time systems`
- **Company:** use the current public affiliation only if it is appropriate to disclose
- **Location:** use the current city rather than listing multiple historical locations
- **Website:** add the personal site only after its landing page and mobile layout are ready

## Pinned repositories

Keep the existing four repositories in this order:

1. `agentKV`
2. `cuebee-infra`
3. `cuebee-speaker-serving`
4. `UAIassist`

## Repository descriptions

Use these concise descriptions in each repository's **About** field:

- **agentKV:** `Application-aware KV-cache lifecycle control for tool-using agents on vLLM V1.`
- **cuebee-infra:** `Stateful streaming inference for revisable transcripts and long-lived multimodal sessions.`
- **cuebee-speaker-serving:** `Multi-tenant real-time speaker diarization with cross-session micro-batching and stable speaker identities.`
- **UAIassist:** `Stateful agent workflow for multi-source spreadsheet processing and report generation.`

## Repository topics

- **agentKV:** `llm-serving`, `vllm`, `kv-cache`, `gpu-memory`, `inference-engine`, `agent-runtime`
- **cuebee-infra:** `streaming-inference`, `stateful-llm`, `vllm`, `speech-to-text`, `multimodal`
- **cuebee-speaker-serving:** `speaker-diarization`, `onnx-runtime`, `microbatching`, `real-time-audio`, `vad`
- **UAIassist:** `langgraph`, `document-processing`, `spreadsheet-automation`, `multi-agent`, `report-generation`

## Publishing

The profile README becomes visible when a public repository named exactly
`lululuyuanyuanyuanGe` contains `README.md` at its root. The current GitHub command-line
login must be renewed before this local directory can be published.
